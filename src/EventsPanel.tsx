import * as React from 'react';

class EventsPanel extends React.Component {
  render() {
    return (
      <div>
        <div>[]Realtime (x) / []Snapshot (x) / []Subscription (x) / []Cache (x) / []Info (x)</div>
        <div> event 1</div>
        <div> event 2</div>
      </div>
    );
  }
}

export default EventsPanel